[project]
name = "spotipyFree"
version = "0.1.0"
description = "A Spotipy-compatible wrapper using SpotAPI"
requires-python = ">=3.8"
dependencies = ["spotapi"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
